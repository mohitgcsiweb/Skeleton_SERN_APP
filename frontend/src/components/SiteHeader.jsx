import React from 'react';

const SiteHeader = () => {
  return (
    <header className='m-2 alert alert-danger text-center'>
     This is a development environment
    </header>
  );
};

export default SiteHeader;